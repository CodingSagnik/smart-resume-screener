# Application Settings
PROJECT_NAME="Smart Resume Screener API"
API_V1_STR="/api/v1"
ENVIRONMENT="development"
DEBUG=True

# Server
HOST="0.0.0.0"
PORT=8000

# Security & JWT
SECRET_KEY="super-secret-key-change-this-in-production-use-openssl-rand-hex-32"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database Configuration
# Default to SQLite for zero-setup local dev; switch to PostgreSQL for production
DATABASE_URL="sqlite+aiosqlite:///./smart_resume_screener.db"
# Example PostgreSQL URL:
# DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/resume_screener_db"

# CORS Configuration (comma-separated list of origins)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","http://localhost:8000"]

# File Upload Configuration
UPLOAD_DIR="./uploads"
MAX_UPLOAD_SIZE_MB=10
ALLOWED_EXTENSIONS=["pdf","docx","doc","txt"]

# LLM Parser Configuration (Gemini or OpenAI)
LLM_PROVIDER="gemini"
GEMINI_API_KEY="your-gemini-api-key-here"
GEMINI_MODEL="gemini-1.5-flash"
OPENAI_API_KEY="your-openai-api-key-here"
OPENAI_MODEL="gpt-4o-mini"
